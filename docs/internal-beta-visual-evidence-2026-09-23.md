# Beta interna sintética — evidencia visual parcial (23-09-2026)

Esta prueba se hizo en un navegador sobre Render LIVE `25f10b4566e1b8d809e3660e69528c0bae6454a0`, no únicamente por API. Todos los hechos, fechas, importes, nombres y referencias introducidos fueron ficticios. No se subieron documentos ni se contactó con terceros. El registro de envío y respuesta fue una simulación dentro del expediente.

## Preflight

- GitHub `main` y Render LIVE coincidían en el SHA completo anterior. El deploy automático `dep-dapm753m8hqs73ag34rg` estaba `live`; Database Migrations y MECORRESPONDE CI estaban verdes para ese SHA.
- `/health`: `status=ok`, `families=26`, `runtime_revision` igual a LIVE.
- El preflight desplegado anterior de almacenamiento, privacidad e indexación sigue documentado en `internal-beta-manual-evidence-2026-09-23.md`. No se infiere de la pantalla una nueva auditoría de esos endpoints o del backoffice.

## Recorrido visual efectuado

En el navegador integrado se abrió la portada: aviso visible de pruebas sintéticas y enlaces a 26 guías, agrupadas por vertical. Se abrió la guía E02-A: título y explicación legibles, CTA «Analizar mi caso», referencia oficial y cautela expresa de no decidir automáticamente. Desde esa CTA se abrió un expediente E02-A ficticio con factura de 150 € y cantidad debida de 100 €. Las preguntas guiadas pidieron fecha y ambos importes; el diagnóstico mostró base alta y 50 € reclamables. Sin embargo, no mostró ningún siguiente paso. Por ello el escenario A no se puede dar por completado en LIVE.

Un relato ad hoc ambiguo sobre una factura de luz entró en `HUMAN_REVIEW` en vez de producir una conclusión automática. Se observó el bloqueo de uploads. La clasificación concreta de ese relato no se considera error sin una revisión de los hechos que el Motor interpretó.

El recorrido completo se hizo con un segundo expediente E04-B ficticio, `4396f1fa-7e1e-47cd-99c9-fe6cde17d40a`:

1. Desde el ejemplo de mantenimiento cobrado tras cambiar de compañía, la UI pidió fecha de fin de suministro (`2026-06-03`), identidad del servicio (`Servicio Ficticio`), contratación original, vinculación al suministro y ausencia de solicitud de conservarlo.
2. Se introdujo un cargo ficticio de 8,99 €, fecha `2026-07-04`, periodo `2026-06-04` a `2026-07-03`, confirmando que sus datos podían verificarse en un documento ficticio en posesión de la persona. La UI mostró diagnóstico `HIGH`, importe reclamable 8,99 € y acción de reclamación. El borrador mostró una fuente oficial verificable.
3. Se registró un envío simulado mediante `web_form`, fecha `2026-09-23`, referencia `PRUEBA_INTERNA_SINTETICA_VISUAL_20260923`; no hubo envío externo.
4. Se registró una respuesta empresarial simulada favorable, con cancelación y devolución prometidas, fecha `2026-09-23`, referencia `RESPUESTA_SINTETICA_20260923`. El caso pasó a «Aceptado · pendiente de cumplir».
5. Se confirmó solo la cancelación: recuperado 0 €, devolución pendiente. La UI conservó «Aceptado · pendiente de cumplir», mostró el compromiso pendiente y añadió un evento parcial. Tras recarga persistieron los datos y el estado.
6. Se confirmó la devolución ficticia de 8,99 € y que no quedaban compromisos materiales. La UI pasó a «Resuelto», mostró el canal e importe, la documentación en solo lectura y la secuencia parcial → resolución una sola vez. Tras recarga el cierre seguía persistido.

Resultado: PASS visual para el lifecycle favorable, cumplimiento parcial, cierre y reentrada anónima de este caso E04-B. No equivale a A–H completo ni a 26 familias recorridas manualmente.

## Cobertura de la matriz y límites

Las pruebas automatizadas `test_beta_acceptance_matrix.py`, `test_guided_question_acceptance.py`, `test_all_family_response_loop.py` y `test_all_claim_provenance.py` recorren las 26 familias registradas para intake/diagnóstico, preguntas guiadas, acción y fuente jurídica, respuesta favorable y resolución; la respuesta parcial no duplica la reclamación inicial. Las rutas de alcance no admitido no están cubiertas manualmente para las 26 familias: existe prueba automatizada específica para cinco familias de compras fuera del ámbito de consumo. Las 26 páginas de guía se verificaron como enlaces visibles en la portada, pero solo E02-A se inspeccionó visualmente en detalle.

Estado del runbook manual: A parcial y bloqueado en LIVE tras diagnóstico E02-A; B, C, D, F y G pendientes; E parcial (se observó `HUMAN_REVIEW` de un relato ambiguo, no el flujo completo tras respuesta); H parcial (cierre y lectura, no todas las mutaciones terminales ni `CLOSED_UNSUPPORTED`). No hay evidencia para afirmar que toda la matriz visual esté superada.

## Hallazgos y corrección preparada

- P2 corregido en la rama de este documento: «¿Me compensa?» mostraba `YES_IF_LOW_COST` en el diagnóstico LIVE. Se añadieron etiquetas legibles para todos los códigos existentes y un fallback conservador para los desconocidos.
- P2 corregido en la rama de este documento: el formulario de cargos mostraba tres controles de fecha sin etiquetas visibles. Ahora distingue fecha del cargo, inicio y fin del periodo; se conservan los nombres y semántica de la API.
- P1 corregido en la rama de este documento, pendiente de CI y despliegue: las guías enlazan a `/demo/`, que servía el HTML estático sin el cargador de módulos seguros. El Motor diagnosticó E02-A correctamente, pero no apareció la acción «Qué hago ahora» ni el botón para preparar la reclamación. La raíz `/` sí cargaba esos módulos, ocultando el defecto en la primera prueba E04-B. `/demo/` y `/demo/index.html` se sirven ahora con el mismo cargador y la primera capa de privacidad fail-closed, conservando `noindex`.
- P2 pendiente: «Ver base jurídica aplicada» muestra identificadores y resultados internos (`ELEC_ADDON_END_WITH_SUPPLY / APPLIES`) en vez de explicación de cara al usuario. El borrador sí muestra una fuente oficial, pero la sección de diagnóstico necesita copy contextual revisado antes de usarla como explicación jurídica pública.
- P0: ninguno nuevo demostrado en esta prueba parcial. Tampoco se declara su ausencia en los escenarios sin ejecutar.

La tanda dirigida tras los dos primeros cambios: 26 tests aprobados. Tras el arreglo P1, la tanda de navegación, privacidad, cargador y matriz de familias: 33 aprobados. La suite amplia local previa dio 668 aprobados y tres fallos ambientales: dos tests de integración PostgreSQL ejecutados con `sqlite:///:memory:` por falta de base efímera local, y la aserción POSIX `0600` sobre NTFS. No se sustituyen por una afirmación de suite local totalmente verde; CI Linux del PR debe decidir la integración.

## Siguiente bloque ejecutable

Pasar esta corrección por CI y verificar la interfaz tras auto-deploy si se integra. Después ejecutar los escenarios manuales restantes A–H con datos sintéticos, empezando por E02-A, cuenta ficticia/reentrada y seguimiento externo, y recorrer la matriz visual de las 26 familias para descartar dead-ends visibles. Mantener prohibidos datos reales, uploads, indexación pública y cualquier decisión jurídica o empresarial inventada.

## Continuación después de PR #211

PR #211 se fusionó en `main` como `8279211c5eec1a02fb8d87d7db64ca2e4996dda8`. `MECORRESPONDE CI` y `Database Migrations` concluyeron correctamente en ese SHA. En la comprobación pública posterior, `/health` devolvió `status=ok`, `families=26` y `runtime_revision` idéntico a `main`. `/health/persistence` indicó PostgreSQL persistente; `/health/storage`, almacenamiento local no persistente y uploads bloqueados. `/privacidad` continuó en 503 fail-closed y `noindex`; el sitemap no se publicó. La URL `/demo/` cargó la interfaz enriquecida y conservó `noindex`. El registro del deploy y los logs de Render no se volvieron a inspeccionar directamente porque la integración MCP exigió elegir una cuenta de trabajo y esa elección aún no se ha confirmado. No extrapolar la salud HTTP a una auditoría de logs.

En navegador real sobre ese LIVE, se repitió el escenario A entrando desde la guía E02-A. El expediente ficticio `9403f6ed-1d88-4e22-be1a-fafd4652aa0b` pasó de 150 € facturados/100 € debidos a un diagnóstico alto de 50 € reclamables; la acción se pudo preparar y mostró fuente oficial. Se registraron **solo dentro del producto** un envío y respuesta favorables simulados, una corrección parcial con devolución de 50 € pendiente y, tras una recarga que conservó ese pendiente, una devolución ficticia de 50 € y cierre. Una segunda recarga conservó resolución, historial y solo lectura. No se contactó con empresa ni organismo alguno.

Para el escenario B, el expediente ficticio E02-A `2200f950-b729-415e-8ae6-5f110e2670d3` con 100 € facturados y 100 € debidos mostró diagnóstico bajo, 0 € reclamables y «Análisis concluido · no hay una acción adicional». No obstante, la pantalla promete reanálisis si aparece un hecho verificable y no ofrece una forma visible de corregir los hechos existentes después de esa conclusión. La API sí acepta la corrección y anula la decisión anterior (`test_informational_action_completion.py`), pero esto **no equivale** a un recorrido de usuario completado. Clasificación provisional: P1 de reentrada/corrección, pendiente de arreglo y prueba visual.

Para D se probó C02 con el expediente ficticio `f22ff7ac-9880-4abe-9ad9-cfe4a3f320b4`: una lavadora reparada que inicialmente funcionaba produjo `MONITOR_CONFORMITY` y la pregunta visible de seguimiento. Al responder que la falta reapareció, la API devolvió el caso a `INTAKE`, sin decisión ni acción actuales y con la siguiente pregunta sobre el defecto; el comportamiento de backend coincide con `test_c02_monitor_followup.py`. Pero la UI siguió mostrando el diagnóstico bajo antiguo y su razonamiento contrario al nuevo hecho, incluso después de recargar. `refresh()` en `backend/app/static/index.html` dibuja `decisions[0]` sin comprobar `current_decision_id`; `case_next_step.js` también toma la primera decisión histórica. Clasificación: P1 por conclusión obsoleta mostrada como vigente. E06 y el resto de D no se han completado visualmente.

Estado tras esta continuación: A visual PASS con datos sintéticos; B y D parciales con P1 abiertos; E04-B favorable/parcial/cierre PASS en su ruta ya descrita; C, E completo, F y G siguen pendientes; H probado para resolución y recarga, pero no exhaustivamente para todas las mutaciones terminales ni `CLOSED_UNSUPPORTED`. No se ha completado inspección manual visual de las 26 familias. Las pruebas automáticas de las 26 familias y la CI no eliminan estos dos P1 de interfaz.

## Continuación después de PR #212: revisión y QA LIVE

PR #212 se revisó en sus 12 archivos: head `1bb8fa89a2425da554f7c5db43efadb4668d7c96`, no draft, fusionable y cuatro checks verdes. Una tanda local dirigida adicional dio 20 aprobadas. Se fusionó sin borrar rama; `main` pasó a `a02ec013275877255e7135a8869df60a9f6faf27` y ambos flujos de CI de `main` terminaron `SUCCESS`. Render inició por auto-deploy `dep-daq0p0ugekts73cr42tg`, `new_commit`, y lo declaró `live`; no hubo despliegue manual. `/health` respondió 200 con `status=ok`, `families=26` y `runtime_revision` idéntica al SHA completo. Los logs de arranque mostraron la misma revisión, `database_backend=postgresql persistent=True`, `synthetic_internal_beta_ready=True`, `internal_beta_blockers=none` y ningún log `error` en la ventana comprobada. `/health/storage` mantuvo `backend=local`, `persistent=false`, `uploads_allowed=false`; `/privacidad` permaneció 503/no-store/noindex, sitemap 404 y `/demo/` noindex.

En C02, el expediente nuevo ficticio `52c38793-757a-4d42-97ed-8c15b8ed7efb` llegó a `MONITOR_CONFORMITY` sin nueva falta. Al indicar después que la lavadora volvía a fallar, pasó a `INTAKE`, preguntó por el defecto y ocultó el diagnóstico bajo antiguo en la tarjeta principal; se comprobó también tras recarga. Tras completar descripción, origen y remedio preferido, apareció un diagnóstico nuevo alto. **P1 residual:** mientras estaba en `INTAKE` sin decisión vigente, «Calidad del expediente» seguía diciendo «Diagnóstico disponible» y «Reglas evaluadas en la decisión actual: 1». Persistió tras recargar. La causa está en `backend/app/case_quality.py`: `build_dossier_quality()` usa `decision_rows[0]` como fallback cuando `case.current_decision_id` es nulo o inválido. El mismo síntoma apareció en E02-A y E06 tras invalidar su decisión. Esperado: readiness de intake y cero reglas de decisión actual, conservando la decisión histórica solo como historial.

En E02-A, el expediente ficticio `f128536a-907d-4399-8ec4-82c0351d2cfd` mostró conclusión informativa con 100 € facturados/100 € debidos y el control visible «Corregir datos». Desde ese control se cambió el importe debido a 80 € en el **mismo** expediente; la UI ocultó el diagnóstico anterior, volvió a intake, conservó las dos versiones del hecho y, tras recarga y reanálisis, mostró 20 € reclamables. Esta ruta específica de corrección funciona en LIVE. La tarjeta de calidad quedó afectada por el P1 residual anterior durante el intake.

En D, el expediente E06 ficticio `8bdf3b9f-09ea-4d22-ba17-6569d6fdb5fa` mostró la pregunta de comparación con lectura real. Al indicar que la factura no coincidía, volvió a intake sin la tarjeta de diagnóstico antigua y, al reanalizar, el Motor lo reclasificó a E02-A y pidió los datos de facturación. La tarjeta de calidad volvió a contar una regla histórica como actual durante el intervalo sin decisión vigente.

En C, un pedido C04 ficticio `4b93bce5-8b4d-4efb-a396-165c370df1d9`, fechado 2026-09-15 y sin fecha pactada, produjo `WAIT_UNTIL_DELIVERY_DUE` con hito calculado por el Motor 2026-10-15. La UI explicó la espera. Pulsar «Comprobar de nuevo» antes del hito dejó el expediente en esa fase; no se alteró el reloj ni se simuló el vencimiento. Este subescenario es PASS visual parcial, no prueba manual del reanálisis al vencer.

Como muestreo multivertical, se inspeccionaron las guías de telecom T01, compras C01, viajes V01, banca B01, alquiler R01, seguros S01 y automoción A01, además de la entrada de energía E02-A: título, CTA hacia `/demo/`, aviso de que la página no decide el caso y sección de fuentes oficiales estaban visibles. No equivale a recorrer 26 expedientes. En un C01 ficticio no consumidor `1e1715b1-57df-4372-a7cf-1686fc61a788`, la UI detuvo la automatización en revisión humana, sin preparar reclamación; mostró `OUT_OF_SCOPE` como código crudo (P2 de copy).

En E, sobre el mismo E02-A corregido se registraron solo en el producto un envío y una respuesta ambigua ficticios. El caso quedó en `HUMAN_REVIEW`, sin cierre automático. La pantalla ofreció «Descargar expediente estructurado» y, al pulsarlo, confirmó «Expediente preparado»; esto no verifica el contenido descargado ni el backoffice de revisión estructurada.

En F apareció un **P0 de seguridad de decisión para beta**: el expediente ficticio E02-A `3e6d097c-5d28-48d1-bc44-f36a6d6d2285` (150 € facturados, 100 € debidos) registró un envío simulado y esta respuesta sintética explícita: «Rechazamos su reclamación y no devolveremos los 50 euros solicitados». LIVE la clasificó como aceptación, pasó a «Aceptado · pendiente de cumplir», ofreció «Verificar cumplimiento» y añadió al historial una respuesta favorable. Esperado: nunca `ACCEPTANCE` ni `VERIFY_EXECUTION` para esa negación; si no hay argumento estructurado seguro, revisión humana. La reproducción local de `DeterministicAlphaGateway.analyze_response()` también devuelve `ACCEPTANCE` para «No aceptamos su reclamación» y «No procedemos a devolver el importe». `backend/app/engine/gateway.py` busca subcadenas positivas (`aceptamos`, `devolveremos`, `procedemos a devolver`) antes de detectar su negación. No hubo comunicación externa ni dinero real. No probar este camino con datos reales; bloquear beta hasta corregirlo y repetirlo visualmente.

Otros P2 visibles: «Ver base jurídica aplicada» expone `ELEC_OVERBILL_REFUND / APPLIES`; en C02 se mostró un contraargumento como `PRICE REDUCTION REQUIRES PROPORTIONAL VALUATION`, y el C01 fuera de alcance mostró `OUT_OF_SCOPE`. Son códigos internos, no copy jurídico suficiente. No se alteraron fuentes ni contenido legal.

Estado A–H ahora: A PASS sintético previo; B ruta de corrección E02-A PASS salvo tarjeta de calidad; C espera pre-hito PASS parcial; D seguimiento C02/E06 PASS salvo tarjeta de calidad; E respuesta ambigua → revisión humana PASS parcial (backoffice pendiente); F FAIL por falso positivo de aceptación; G pendiente (la creación de cuenta exige confirmación puntual del usuario si se ejecuta por UI); H parcial de resolución/solo lectura previo. La matriz visual dirigida revisó ocho verticales a nivel guía y varios recorridos de riesgo, no 26 casos completos. No congelar beta interna.

## Continuación después de PR #213: corrección y repetición LIVE

PR #213 fue revisada abierta, no draft y fusionable en head `fbaf2c91c099a129f290a9da03c1b9a82cee2043`, con cuatro checks obligatorios `SUCCESS` y sin cambios inesperados fuera de clasificador, calidad, tests y docs. Se fusionó como `be7a970de9ed0ff13da7fa9decedb69794abddc7`; los dos flujos de CI de ese `main` terminaron `success`. Render auto-deploy `dep-daqbb1o473hc738m5cjg` (`new_commit`) terminó `live` en el mismo SHA completo, sin despliegue manual. `/health` devolvió 200, `status=ok`, `families=26` y revisión exacta. Logs de arranque: misma revisión, PostgreSQL persistente, `synthetic_internal_beta_ready=True`, `internal_beta_blockers=none`, uploads desactivados e indexación pública desactivada; no aparecieron logs de nivel error en la ventana revisada. `/health/storage` continuó `blocked`/local no persistente; `/privacidad` 503/no-store/noindex, sitemap 404 y `/demo/` noindex.

F se repitió visualmente en E02-A ficticio `2aa5bd6e-2733-4c2a-bcc6-283108dd5846` con 150 € facturados y 100 € debidos. Tras preparar la reclamación y registrar **solo en el producto** un envío simulado, se incorporó la respuesta ficticia «rechazamos su reclamación y no devolveremos los 50 euros solicitados», ref. `RESPUESTA_SINTETICA_PR213_20260924`. LIVE quedó en `HUMAN_REVIEW`, presentó «Este punto está detenido para revisión humana» y no ofreció «Verificar cumplimiento», cierre favorable ni repetición de la reclamación inicial. Es el mismo patrón de negación que antes daba el falso `ACCEPTANCE`; P0 cerrado en esta prueba dirigida, no evidencia universal para todo lenguaje libre.

B/E02-A se repitió en el expediente ficticio `7b8037e1-2f63-44c0-9895-a2c211de6d05`: 100/100 produjo base baja y control «Corregir datos». La corrección a 100/80 en el mismo expediente lo devolvió a intake; «Calidad del expediente» indicó «Recopilando hechos» y cero reglas evaluadas en la decisión actual, sin diagnóstico bajo histórico como vigente. Persistió tras recarga. El nuevo diagnóstico calculó 20 € y una regla actual.

D/C02 se repitió en `80d33fc7-a48b-45a8-9352-dfc6f1c2e208`: lavadora reparada sin defecto nuevo → base baja y pregunta de seguimiento. Tras indicar que apareció otra falta, intake preguntó por el defecto, desapareció la base baja previa y la calidad volvió a «Recopilando hechos» con cero reglas actuales; persistió tras recarga. D/E06 se repitió en `31d3c574-dbdc-450d-991d-fe8aa4510556`: lectura estimada por fallo remoto con lectura real posterior → base baja y comparación visible. Responder que no coincidía devolvió a intake, ocultó diagnóstico viejo y la calidad mostró cero reglas actuales, también tras recarga. No se completó el reanálisis monetario de esta segunda repetición E06.

Estado post-#213: P0/P1 anteriores cerrados **en sus recorridos visuales dirigidos**. La beta sigue `NOT YET`: E backoffice, G cuenta/reentrada, H mutaciones terminales y `CLOSED_UNSUPPORTED`, vencimiento de C04 y la matriz de expedientes por familia requieren más evidencia. Los códigos internos P2 siguen visibles. Sin datos reales, envíos externos, coste, uploads o indexación pública.

## Continuación del 24-09-2026: E, H y matriz dirigida

Preflight antes de esta prueba: GitHub `main` y Render LIVE coincidían en `dc8fd9e6e821b0101b60520171b49729b3193784`; auto-deploy `dep-daqbo90473hc738mi7ng` en estado `live`. `/health` respondió 200, `status=ok`, `families=26` y `runtime_revision` idéntica. No se lanzó un deploy manual.

- **E, respuesta ambigua:** en el caso sintético E02-A `633884c6-8ca9-4806-a54b-159013822442` se diagnosticaron 50 € ficticios (150 € facturados, 100 € debidos), se registraron solo en el producto un envío ficticio `ENVIO_FICTICIO_QA_E_20260924` y la respuesta «Acusamos recibo de su solicitud. Estamos revisando la factura y responderemos más adelante», ref. `RESPUESTA_FICTICIA_QA_E_20260924`. LIVE detuvo el caso en `HUMAN_REVIEW`, sin cierre favorable automático. El JSON descargado de **ese expediente concreto** incluyó caso, hechos y versiones, decisión y evaluaciones, procedencia de fuente jurídica, acciones, comunicaciones, revisión humana `OPEN` con motivo `UNRECOGNIZED_COMPANY_RESPONSE` y resultados vacíos; no incluyó documentos ni secretos. La UI muestra historial, pero el paquete no tiene un campo de cronología permitido explícito: mejora P2 de la exportación. `/backoffice/` existe y exige token; `/api/admin/reviews?status=OPEN` sin credenciales devolvió 401. Por tanto la gestión visual del backoffice **no está verificada**; las pruebas locales dirigidas del control administrativo sí pasaron. La tarjeta de diagnóstico conservaba el importe anterior con la etiqueta «Dinero reclamable ahora» durante la revisión: P2 de contexto.
- **H, cumplimiento y cierre:** caso sintético E02-A `6bf9db88-344c-4227-8ef1-9a7d6acd6ccd`, mismo cálculo de 50 €. Tras envío ficticio `ENVIO_FICTICIO_QA_H_20260924` y aceptación ficticia `RESPUESTA_FICTICIA_QA_H_20260924`, LIVE permaneció `RESOLVED_PENDING_EXECUTION`, no cerró por una promesa. Solo tras registrar una devolución ficticia verificada de 50 € pasó a `RESOLVED`; conservó importe, canal, historial y documentación de solo lectura. La reentrada al expediente mantuvo ese estado y no mostró controles para corregir hechos, diagnosticar, preparar acción o incorporar respuesta. El canal aparecía como código `invoice_credit_or_rebilling`: P2 de copy. Las pruebas locales verificaron además que mutaciones de hechos, diagnóstico, preparación, respuesta y uploads quedan bloqueadas tras cierre; no se intentó forzar mutaciones por UI en producción. `CLOSED_UNSUPPORTED` no apareció naturalmente: el clasificador de temas desconocidos envió el caso sintético `5ebf3dec-318d-47c8-8c23-1379d3368585` a revisión asistida. Su guarda terminal está cubierta por pruebas locales, **no** por un recorrido visual LIVE.
- **Matriz de ocho verticales, selección dirigida:** energía E02-A (ambigüedad y cierre arriba); compras, caso `e9437c5b-5c94-4ca1-915f-d6ab9f2410f6`, abrió preguntas de garantía; telecom `def223ac-2ff8-4ef4-8414-79d059803923`, viajes `b8c81d6a-c13f-41ea-b591-f85efc7af2db`, banca `b7e251ca-1cce-43c5-a084-f04b5fe4cded`, seguros `5374997c-c8a5-4089-a509-023d2224ccd4` y automoción `35d9948f-1518-4464-bd08-061bc60e4852` entraron en revisión asistida sin conclusión ni acción automática; alquiler `fb85289c-19bf-45d5-afb4-8d6e04bd56aa` abrió preguntas de fianza. En alquiler se observaron opciones internas en inglés (`dwelling`, `other urban use`, etc.): P2 de texto visible. Es una comprobación de intake y guardas, **no** una aprobación manual de 26 familias ni de la resolución completa en las ocho verticales.
- **G:** se inspeccionó el diálogo «Guardar este expediente» con email y contraseña; no se creó cuenta ni se aceptaron términos. La reentrada entre dispositivos sigue sin verificar. No se introdujeron emails ni otros datos personales reales.

Los P2 de copy observados (canal de resultado, diagnóstico histórico, opciones de alquiler, códigos internos de diagnóstico) se corrigieron localmente en la rama `fix/beta-readable-internal-copy`, sin cambiar estados, reglas jurídicas ni datos. La tanda dirigida tras la corrección dio 18 aprobadas; la segunda suite completa de Windows dio **688 aprobadas y 1 excluida**, siendo la excluida únicamente la comprobación POSIX de permisos `0600` que NTFS no representa. CI Linux y despliegue de esa rama deben verificarse por separado antes de atribuirle efecto a LIVE. E backoffice, G y `CLOSED_UNSUPPORTED` visual impiden declarar `BETA INTERNAL STATUS: PASS`.

**Rectificación post-#215:** PR #215 se integró y Render LIVE alcanzó `b69b7277bbc9719fadd50dab7104dca7e8521097` con salud y guardas correctas (detalle en `AI_HANDOFF.md`). Al repetir visualmente el caso de alquiler `fb85289c-19bf-45d5-afb4-8d6e04bd56aa`, las opciones **seguían en inglés**. La causa es que `guided_question_inputs.js` sustituye `inputFor` después de cargar el HTML y usaba `o.label` directamente; la prueba anterior solo inspeccionaba el HTML. Por tanto el P2 de opciones **no quedó cerrado con #215**. La rama `fix/beta-choice-labels` corrige el módulo ejecutado y añade una prueba Node de etiquetas visibles y valores conservados; su tanda dirigida dio 10 aprobadas. Falta integrar y repetir en LIVE antes de cerrar este hallazgo.

**Rectificación post-#216:** PR #216 se integró como `872329b6f4fd55b1959523b7e367b27cea67d78e` y Render auto-deploy `dep-daqlqfrncjis73aotbbg` terminó `live`; `/health` marcó la misma revisión. El archivo JavaScript consultado directamente en LIVE contenía `choiceLabel(o)`, pero la reentrada al caso y un caso de alquiler ficticio nuevo `958ca47f-c5fc-40a1-9b5b-b0d10629b355` **seguían mostrando etiquetas inglesas**. No se ha demostrado aún que el navegador ejecute el módulo nuevo. Hipótesis: URLs de assets sin versión retenidas en caché. `fix/beta-runtime-asset-revision` versiona el cargador y todos los módulos con el SHA completo de runtime; 18 pruebas dirigidas pasaron, incluida ejecución Node del cargador. Pendiente CI, integración y repetición visual; si persiste, no asumir caché como causa confirmada.

**Cierre visual post-#217:** PR #217 se integró como `eacf36a2826a268eea0b139126dd9fcc899c7b17`; cuatro checks de PR y cuatro del merge commit verdes. Render auto-deploy `dep-daqm0mmgekts73e5t9kg` terminó `live` en ese SHA y `/health` devolvió 200, `status=ok`, `families=26`, revisión exacta. En un nuevo expediente de alquiler ficticio `180ca2f6-19cf-4c4b-ac2d-88bb3dbb1413`, el navegador cargó `dossier_quality.js` y `guided_question_inputs.js` con `?v=eacf36a2826a268eea0b139126dd9fcc899c7b17` (confirmado también en logs). La pregunta «¿Qué tipo de alquiler terminó?» mostró «Vivienda», «Otro uso urbano», «Solo una habitación», «Alquiler turístico u hospedaje» y «No lo sé», manteniendo los valores internos `dwelling`, `other_urban_use`, `room_only`, `tourist_or_hospitality`, `unknown`. Así queda cerrado **este P2 observado**, no una auditoría de todas las opciones del producto. Logs de arranque con PostgreSQL persistente, `synthetic_internal_beta_ready=True`, `internal_beta_blockers=none`, uploads desactivados e indexación desactivada; no hubo logs de nivel error en la ventana revisada. `/health/storage` continuó `blocked`/local no persistente y `/privacidad` 503/no-store/noindex; `/demo/` siguió noindex. La beta interna permanece `NOT YET` por E backoffice, G, `CLOSED_UNSUPPORTED` visual y profundidad de la matriz.

**Compras y alquiler post-#219:** en C01 ficticio `e9437c5b-5c94-4ca1-915f-d6ab9f2410f6`, una cafetera nueva de 80 € que dejó de funcionar a los diez días, comprada por particular a profesional, pasó por diez hechos guiados. El diagnóstico mostró base alta, 0 € reclamables ahora y 80 € de valor protegido, sin presentar ese precio como devolución inmediata. Se preparó **solo dentro del producto** una solicitud de puesta en conformidad sin coste, con fuente oficial BOE para los artículos 117–121; no se pulsó «Ya lo he enviado» ni hubo comunicación externa. Antes de #219, la sección «Qué podría tumbar la reclamación» mostraba como negativo un hecho favorable de negativa del vendedor, y ambos aspectos se ocultaban bajo «Aspecto a revisar». En R01 ficticio `180ca2f6-19cf-4c4b-ac2d-88bb3dbb1413`, vivienda entregada supuestamente el 1 de septiembre sin prueba de la fecha ni saldo reconocido, el Motor se detuvo en `HUMAN_REVIEW`, sin cuantía ni acción automática; en sus preguntas se observaron más opciones inglesas de fianza y saldo.

PR #219, solo HTML de copy y pruebas, pasó sus cuatro checks y se integró como `af61aa5dde1c7564511819d5e62b25228e8e5490`. Render auto-deploy `dep-daqmci6q1p3s73an07qg` terminó `live` en el mismo SHA; `/health` 200, `status=ok`, `families=26`, revisión exacta, y los cuatro checks del merge commit quedaron verdes. Logs de arranque: misma revisión, PostgreSQL persistente, `synthetic_internal_beta_ready=True`, `internal_beta_blockers=none`, uploads e indexación desactivados, sin logs de nivel error en la ventana revisada. En reentrada C01, la sección pasó a «Aspectos que pueden influir en la reclamación» y distinguió una excepción posible de la negativa del vendedor «Confirmado según los datos declarados». En R01 ficticio `fb85289c-19bf-45d5-afb4-8d6e04bd56aa`, las preguntas de fianza y saldo mostraron «Fianza legal en metálico», «Garantía adicional», «Mixto o no lo sé», «Importe confirmado» y «Hay descuentos o se discute el importe»; sus valores internos permanecieron sin cambios. Los P2 observados están cerrados en estas rutas, no en todas las opciones de las 26 familias. E backoffice autenticado, G, `CLOSED_UNSUPPORTED` visual y más profundidad de matriz mantienen `BETA INTERNAL STATUS: NOT YET`.

## Cierre dirigido del 25-09-2026: G, C04 y criterio de suficiencia

Baseline comprobado antes de esta rama: GitHub `main`, último Render LIVE (`dep-daqmh37f3r2c73ac93h0`) y `/health.runtime_revision` en `f97f0a70e3f0f232123e41e2686249d283b867b2`; `/health` 200, `status=ok`, `families=26`; cuatro checks de `main` `success`. Logs de arranque con esa revisión, PostgreSQL persistente, `synthetic_internal_beta_ready=True`, `internal_beta_blockers=none`; no hubo logs `error` en la ventana consultada.

- **E:** se mantiene el caso ambiguo y exportación estructurada descritos arriba. La única API de Render disponible para esta verificación expone servicio, logs, despliegues y consultas PostgreSQL de lectura, pero no una ejecución dentro del servicio que reutilice `ADMIN_API_TOKEN` sin extraerlo. No se pidió ni leyó el secreto. El endpoint administrativo sin token permanece protegido (401). **BACKOFFICE_ACCESS_TOOLING_BLOCKER**: falta repetir la vista LIVE autenticada, no hay defecto funcional demostrado. Pruebas locales de API administrativa, contexto, revisión estructurada y guardas, más la exportación observada, dan evidencia suficiente para la beta sintética dirigida.
- **G:** `/api/auth/capabilities` devolvió `transactional_email_operational=false`, recuperación/verificación no disponibles. El formulario de registro no mostró términos ni aceptación de privacidad. Se creó exactamente una cuenta ficticia `@example.com` con contraseña sintética no documentada y se asoció C04 `36c52f88-1a70-4e85-a685-a80ce1849f04`. La UI registró «Expediente guardado». Se cerró sesión; desde una pestaña nueva sin `#case`, el login mostró únicamente «Pedido no entregado» de esa cuenta, se abrió el mismo ID y persistieron decisión, 0 €/35 €, espera e historial de inicio/guardado/diagnóstico. Es una segunda sesión de navegador, no una prueba física de otro dispositivo. El aislamiento cruzado está cubierto por tests de cuentas y capacidades, no por creación de una segunda cuenta LIVE.
- **C04:** en ese caso ficticio se confirmaron consumidor, vendedor profesional, pedido de 35 €, no entrega, fecha pactada y plazo adicional hasta 2026-10-15. Antes del hito, LIVE mostró «Todavía no toca enviar una nueva acción», 0 € reclamables ahora, 35 € de valor protegido y diagnóstico de espera. «Comprobar de nuevo» devolvió 409 `The waiting milestone has not elapsed yet`; la decisión y los tres hitos de historial se mantuvieron. La guarda funcionó; el texto del alert en inglés es P2 y se traduce en esta rama sin alterar API/lifecycle. Los tests deterministas cubren reentrada posterior, transición a resolución y rollback/atomicidad. El recorrido LIVE posterior al 15-10-2026 queda **TIME_DEPENDENT_FOLLOW_UP**.
- **Unsupported/H:** el clasificador sin familia crea transitoriamente `CLOSED_UNSUPPORTED` en el servicio base, pero la instalación vigente del fallback lo lleva a `HUMAN_REVIEW` antes de exponerlo al consumidor. Para familias conocidas fuera del régimen automatizado, el redirect también se completa y abre revisión protegida. Sin decisión jurídica falsa, cuantía ni reclamación automática. `CLOSED_UNSUPPORTED` público es **NOT_PUBLICLY_REACHABLE_BY_CURRENT_DESIGN**; tests ejercitan su guarda terminal y UI estática. La ruta H favorable hasta `RESOLVED` y reentrada solo lectura sigue demostrada por el caso E02-A descrito arriba.
- **Matriz y criterio:** ocho verticales representadas visualmente en intake; profundidad dirigida en energía (respuesta favorable, rechazo, ambigüedad, corrección, ejecución/cierre), compras (C01 no monetaria y C04 espera económica) y alquiler (información insuficiente/revisión). El resto se detuvo deliberadamente en revisión asistida sin conclusión peligrosa. CI cubre las 26 familias y los invariantes repetitivos. No se crearon 26 casos completos por volumen. La tanda local dirigida dio 41 aprobadas; tras el P2, 21 aprobadas. La primera suite amplia incluyó dos fallos de integración PostgreSQL porque este entorno usa SQLite y no hay base desechable configurada. Repetida la suite funcional pertinente: **691 aprobadas, 1 excluida** (test de modo POSIX `0600` bajo NTFS). Los cuatro checks PostgreSQL/Linux del baseline de `main` siguen verdes. P0=0 y P1=0 conocidos; P2 de cronología explícita en exportación y otras etiquetas no muestreadas quedan en backlog.

**BETA INTERNAL STATUS: PASS**, solo para beta interna con datos ficticios. Este PASS no permite beta real: privacidad/datos empresariales auténticos, recuperación durable de PostgreSQL (la instancia gratuita consultada muestra caducidad 2026-10-15) y almacenamiento documental persistente siguen pendientes. Uploads, privacidad pública e indexación permanecen fail-closed. El aviso P2 de espera solo se atribuirá a LIVE tras CI, merge y auto-deploy de esta rama.

**Repetición post-#221 en LIVE:** cuatro checks de PR y cuatro de `main` `success`; merge commit completo `9ac4b4523d655ce8219a502271a0f49c008ee8f3`. Render inició automáticamente `dep-dar0l4nlk1mc73d0gq6g` y lo cerró `live` en el mismo SHA. `/health` 200 (`status=ok`, `families=26`, revisión exacta); logs de arranque con esa revisión y beta sintética lista, sin `error` en la ventana. PostgreSQL persistente; uploads bloqueados/local no persistente; privacidad 503/no-store/noindex; demo noindex y sitemap 404. El caso C04 ficticio `36c52f88-1a70-4e85-a685-a80ce1849f04` volvió a mostrar 0 € ahora, 35 € protegidos y espera hasta 2026-10-15. «Comprobar de nuevo» mostró **«El plazo de espera todavía no ha vencido. El expediente permanece sin cambios.»** en español; decisión e historial sin cambios. P2 de aviso cerrado en LIVE; **BETA INTERNAL STATUS: PASS** sintético confirmado. La cuenta ficticia quedó desconectada al terminar.
